<view>

   <view class="card" wx:for="{{items}}" wx:key="uid">

    <view data-url="{{item.url}}" bindtap="onItemClick">
      <image class="image" mode="aspectFill" src="{{item.url}}" />
      <view class="title">{{item.desc}} | {{item.pubTime}}</view>
    </view>

  </view> 

</view>